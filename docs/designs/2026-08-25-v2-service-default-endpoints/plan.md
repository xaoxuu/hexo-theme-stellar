---
title: Stellar v2 自部署服务默认 endpoint 与静默降级执行计划
date: 2026-08-25
---

# 执行计划

1. [x] 更新主题默认配置、Schema 默认值、目标元数据和配置测试。
2. [x] 实现 Rating/Vote 的 `null` 静态禁用态，以及 Site Info/Rating/Vote 的静默远程失败降级。
3. [x] 补浏览器失败路径测试并重新生成配置 Reference。
4. [x] 同步主题知识库、公开 Wiki 和主工程 M6 状态记录。
5. [x] 运行主题检查、主工程构建和差异复核。
6. [x] 提交并推送主题 `v2`，评论证据并闭环 #730；主仓库保持未提交。

# 风险与回退

- 公共实例是尽力服务；任何不可用都必须保留静态内容，不能阻塞构建或页面初始化。
- `null` 必须保持显式关闭语义，不能被 Schema 默认重新填充。
- 仅吞掉三项业务 endpoint 的预期远程失败，不扩大到配置、资源加载或程序错误。
