---
title: 列表页置顶内容轮播 检查清单
date: 2026-08-12
---

# 检查清单 / 验证记录

## 验证

- [x] `npm run lint` 通过
- [x] `npm test` 通过
- [x] 主工程 `npm run g` 全量构建通过
- [x] 页面类型覆盖：首页第 1/2 页、归档、标签/分类页、专栏列表、wiki 全量页与 tag 筛选页
- [x] 无置顶内容时全站不渲染轮播，列表行为不变（验证时先无置顶、后置顶对比）
- [x] 单条置顶为静态单张（wiki 单条验证）；多条按 pin 数值排序（4/3/1/0，含 0 也置顶）
- [x] 轮播交互：自动播放、圆点点击、触摸滑动（代码路径）、跨页缓存续播；`prefers-reduced-motion` 经代码审查
- [x] 进度缓存：localStorage 按内容类型分组，切换导航 tab 后续播（CDP 实测）
- [x] 初始化定位到缓存序号时禁用过渡，避免每次进页面播放“从第一张滑过来”的动画
- [x] 初始化前轮播隐藏（visibility:hidden），定位后再显示，避免切换页面/刷新时闪出第一张
- [x] 置顶文章卡片不再显示 pin 图标（轮播开启时）
- [x] 深色模式、窄屏布局（渐变遮罩 + 白字，样式经构建审查）
- [x] 文章幻灯片完全复用列表文章卡片样式（photo/默认卡片），仅固定宽高比（2:1）
- [x] 轮播卡片与列表卡片标题/正文间距一致（flex 手动对齐外边距，实测均为 16px）
- [x] 白底文章卡片（列表卡片与置顶轮播卡片）hover 效果：标题底部主题色下划线高度 0→12px、不透明度 0→0.5、无圆角，层级在文字下层
- [x] 文章卡片 hover 不再有阴影变化（保留静态阴影）
- [x] 移动端（≤667px）置顶轮播白底卡片隐藏正文，标题/元信息保留
- [x] 分页指示器：贴右下角（right/bottom 0、padding 1rem），间距 4px、圆点 8×8px、激活横条 24×8px，未激活 0.5 白、激活纯白、0.1 黑 1px 边框（不随主题变化）；激活横条叠加主题色进度动画（linear，时长=轮播间隔），填满即切换；hover/焦点/隐藏暂停续播
- [x] 滚轮/触控板切换：横向 deltaX 与鼠标滚轮 deltaY 均映射切页，阈值 + 400ms 冷却，轮播区域拦截滚动；触摸松手滑动保留
- [x] `python3 docs/knowledge/tools/verify.py` 知识库硬事实核查通过（行号 0、版本 0）

## 文档同步

- [x] `docs/designs/2026-08-12-pin-slider/` 方案文档已就位
- [x] `docs/knowledge/` 对应领域已更新
- [x] `docs/knowledge/VERIFICATION.md` 已登记
- [x] `languages/` 文案（本次无需新增）
