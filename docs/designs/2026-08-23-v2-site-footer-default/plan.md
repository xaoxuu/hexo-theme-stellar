---
title: Stellar v2 site Footer 默认文案修正执行计划
date: 2026-08-23
---

# 执行计划

## 实施步骤

1. [x] 校正目标契约、Schema 示例与主题 `_config.yml` 默认文案。
2. [x] 更新配置解析断言并重新生成配置 Reference。
3. [x] 同步配置知识库与偏差修正记录。
4. [x] 执行主题检查、知识库核查和主工程构建，回填验证结果。

## 风险与回退

- 风险仅限未显式覆盖 `site.footer.content` 的站点重新出现主题署名；这是恢复迁移前默认行为。
- 显式配置空字符串仍可关闭文案，不新增兼容别名或运行时分支。
