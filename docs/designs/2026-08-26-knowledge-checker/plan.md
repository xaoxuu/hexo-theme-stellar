---
title: 知识库严格门禁执行计划
date: 2026-08-26
---

# 执行计划

## 实施步骤

1. [x] 用正反例固定链接、配置字段、版本与代码围栏契约。
2. [x] 实现 `ci/check-knowledge.js` 与 `npm run knowledge:check` 单一入口。
3. [x] 将 CI、AGENTS、贡献指南和知识库 README 迁移到稳定命令指针。
4. [x] 修复当前失效链接与过时配置引用，清除全部未分类发现。
5. [x] 删除 Python 核查器、报告缓存、无效 ripgrep 安装与相关测试。
6. [x] 根据最终 diff 完成文档同步核验并记录验证结果。

## 风险与回退

- Markdown 正则只处理仓库当前使用的内联链接语法；新增语法前以负向 fixture 固定预期，避免静默扩张启发式范围。
- 配置字段仅对 Theme Scope 顶层根启用严格识别，避免把 ViewModel、Hexo 对象或普通文件名误判为主题配置。
- 若迁移结果与旧报告不一致，以 Schema Reference、配置审计和实际链接目标逐项分类，不保留无理由基线。
