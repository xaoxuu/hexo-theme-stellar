---
title: 侧边栏圆角层曲率对齐执行计划
date: 2026-08-25
---

# 执行计划

## 实施步骤

1. [x] 增加无背景图 glass 侧栏的真实 Stylus 编译回归测试并确认失败。
2. [x] 让 `.leftbar-container:before/:after` 继承容器 `corner-shape`，使测试通过。
3. [x] 同步侧边栏知识库、验证台账与公开 Wiki。
4. [x] 执行主题门禁、主工程构建和 Chrome 四场景验收。
5. [x] 根据最终功能 diff 完成文档同步核验。

## 风险与回退

- 变更只影响伪元素曲率；不支持 `corner-shape` 的浏览器继续回退普通圆角。
- 如出现兼容性问题，可移除伪元素的继承声明，恢复原有绘制路径。
