# 检查清单 / 验证记录

- [x] Topic 未显式覆盖时完整使用全局 Brand。
- [x] Topic 的 `identity.icon`、`name`、`tagline`、`routing.path` 不自动进入 Brand。
- [x] Topic `sidebar.left.brand` 与页面覆盖仍按优先级生效。
- [x] Wiki / Notebook 自动 Brand 行为不变。
- [x] `npm run check` 通过（192 项测试）。
- [x] 主工程 `npm run g` 通过（254 个生成文件）。
- [x] 生成的 Topic 文章 Brand 与站点 Brand 一致，Wiki 仍使用自动 Brand。
- [x] 三个仓库 `git diff --check` 通过。
