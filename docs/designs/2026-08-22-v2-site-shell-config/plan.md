---
title: Stellar v2 site Shell 配置实施计划
date: 2026-08-22
status: 已实施
issue: 706
---

# 实施计划

1. 将 `site` 目标节点标记为 delivered，扩展活动 Schema 以支持派生默认、对象数组和动态记录。
2. 迁移主题与主站 YAML，切换 Brand、菜单、左栏操作和主 Footer 消费链。
3. 将 Brand 运行时/ViewModel 字段统一为 `variant`，保留未迁移内容作用域的构建边界。
4. 更新目录状态、Reference、契约测试、主题知识库、验证台账与主工程 v2 蓝图。
5. 执行主题全检、主工程构建、页面产物抽查与 Standards / Spec 双轨 review。
6. 提交并推送主题 `v2`，登记 SHA 与验证证据，以 `resolved` 闭环 #706。
