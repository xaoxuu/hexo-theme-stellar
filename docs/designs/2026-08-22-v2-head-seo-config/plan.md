---
title: Stellar v2 head 与 SEO 配置执行计划
date: 2026-08-22
status: 已实施
issue: 705
---

# 执行计划

1. 扩展声明式配置 Schema 与解析器，交付 `seo`、`resources.preconnect` 和站点 `inject`。
2. 更新 #704 节点状态，迁移主题默认配置和主工程 `_config.stellar.yml`。
3. 切换 Post ViewModel、head、JSON-LD、脚本注入和浏览器 canonical 上下文消费链。
4. 更新配置 Reference、契约测试、模板静态约束和 Post SEO 回归。
5. 同步主题配置/SEO/canonical 知识库、验证台账、主工程 SEO 记录与 v2 蓝图。
6. 执行主题检查、主工程构建、生成产物抽查和 Standards / Spec 双轨 review。
7. 只提交主题 #705 文件到 `v2`，推送、评论证据并以 `resolved` 标签闭环。
