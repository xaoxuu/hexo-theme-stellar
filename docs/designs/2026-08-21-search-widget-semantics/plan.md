---
title: 搜索控件语义优化执行计划
date: 2026-08-21
---

# 执行计划

## 实施步骤

1. [x] 调整 `search.ejs` 的搜索地标、标签关联、输入类型和无障碍属性。
2. [x] 微调搜索组件样式，保持输入框与图标的既有视觉反馈。
3. [x] 新增模板结构测试，覆盖新旧语义结构。
4. [x] 同步主题知识库、核查记录和主工程 Wiki。
5. [x] 运行主题检查、主工程构建并核验生成 HTML。

## 风险与回退

- `type="search"` 可能带来浏览器原生装饰，通过组件内 appearance 规则消除视觉差异。
- `<label>` 继续使用原 `.search-button` 布局和图标后代选择器；若原生聚焦行为异常，可回退模板元素变更而无需修改客户端搜索接口。
