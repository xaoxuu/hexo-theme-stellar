---
title: Stellar v2 主题配置可发现性执行计划
date: 2026-08-25
---

# 执行计划

## 实施步骤

1. [x] 增加配置可发现性测试，冻结活动字段、行尾示例值和结构契约的覆盖规则。
2. [x] 在 `_config.yml` 补齐 Brand 活动空字段与 Footer、Notebook、contributors 示例。
3. [x] 更新配置知识库和核查记录。
4. [x] 运行主题门禁、知识库核查和主工程构建；完整门禁仅被工作区既有 Alpha 性能 Reference 漂移阻断，其余检查通过。

## 风险与回退

- 主题 `_config.yml` 中的空值用于展示字段，运行时无站点覆盖时仍由 Schema 默认派生；站点 `_config.stellar.yml` 若显式保留空值则按 `null` 覆盖处理。
- 测试只读取配置文本与 Schema，不参与运行时；如需回退，可移除测试和新增注释，不影响已有配置行为。
