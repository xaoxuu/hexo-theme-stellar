<%# 笔记本信息卡片 %>
<article class="md-text">
  <div class="preview"><img src="<%= notebook.identity?.icon || theme.default.project %>" alt="icon"/></div>
  <div class="excerpt">
    <h2 class="post-title"><%= notebook.headline || notebook.name %></h2>
    <% if (notebook.description) { %>
      <p><%= notebook.description %></p>
    <% } %>
  </div>
</article>
