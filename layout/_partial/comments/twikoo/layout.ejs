<%
function layoutDiv() {
  var el = '';
  el += '<div id="twikoo_container" class="with-fancybox"';
  let cfg = {};
  if (page.comments?.id) {
    cfg['comment_id'] = page.comments.id;
  }
  for (let key of Object.keys(cfg)) {
    if (cfg[key]) {
      el += ' ' + key + '="' + cfg[key] + '"';
    }
  }
  el += '><div class="lazy-icon"></div></div>'
  return el;
}
%>

<%- layoutDiv() %>
