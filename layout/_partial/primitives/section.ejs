<%
var allowedSlots = ['cover', 'topbar', 'leftbar', 'rightbar', 'navigation', 'content', 'footer', 'scripts']
if (!allowedSlots.includes(slot)) {
  throw new Error('Stellar Layout Section: 未知 slot ' + slot)
}
var sectionItems = Array.isArray(items) ? items : []
%>
<%- sectionItems.join('') %>
