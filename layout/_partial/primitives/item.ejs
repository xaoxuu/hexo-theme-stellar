<%
var allowedKinds = ['cover', 'brand', 'search', 'navigation', 'widget', 'content', 'footer', 'mask', 'menu-button', 'scripts']
if (!allowedKinds.includes(kind)) {
  throw new Error('Stellar Layout Item: 未知 kind ' + kind)
}
var itemBody = typeof body === 'string' ? body : ''
%>
<%- itemBody %>
