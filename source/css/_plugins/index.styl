@import 'lazyload'
@import 'aplayer'

// 根据主题配置加载
if hexo-config('extensions.features.reveal.enabled')
  @import 'scrollreveal'
@import 'copycode'
if hexo-config('extensions.features.math.provider') == 'katex'
  @import 'katex'
if hexo-config('extensions.features.card_hover.enabled')
  // 最后加载，组合并覆盖 link/grid 既有 hover transform
  @import 'card-hover'
